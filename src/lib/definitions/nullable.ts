export interface NullableString {
  String: string;
  Valid: boolean;
}

export interface NullableDate {
  Time: string;
  Valid: boolean;
}

export interface NullableBoolean {
  Boolean: boolean;
  Valid: boolean;
}
